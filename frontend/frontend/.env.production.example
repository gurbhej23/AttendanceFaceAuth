# Required on Vercel for API + WebSocket calls to work.
# Vercel cannot proxy WebSockets — must point directly at Render.
VITE_API_URL=https://YOUR-BACKEND.onrender.com/api

# Optional explicit WebSocket origin (wss:// or https:// both work)
# VITE_WS_URL=wss://YOUR-BACKEND.onrender.com
