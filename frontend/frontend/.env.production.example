# Recommended on Vercel (overrides the built-in Render default if your backend URL changes)
VITE_API_URL=https://attendancefaceauth.onrender.com/api

# Optional if the Render hostname changes
# VITE_BACKEND_ORIGIN=https://attendancefaceauth.onrender.com

# Optional: WebSocket URL (wss) if not derived from VITE_API_URL
# VITE_WS_URL=wss://attendancefaceauth.onrender.com
