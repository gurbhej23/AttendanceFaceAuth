import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aifaceattendance.app',
  appName: 'AI Face Attendance',
  webDir: 'dist'
};

export default config;
